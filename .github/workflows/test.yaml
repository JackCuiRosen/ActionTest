name: 打包Vue
on: push

permissions:
  contents: write

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
    - name: 读取仓库内容
      uses: actions/checkout@v4
    - name: 显示路径
      run: pwd
    - name: 显示版本
      run: npm --version
    - name: 安装依赖和打包项目
      run: |
        npm install
        npm run build
    - name: 部署🚀
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: gh-pages
        folder: build
    