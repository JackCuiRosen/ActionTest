name: 打包Vue
on: push

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
    - name: 读取仓库内容
    - uses: actions/checkout@v4
    - run: pwd
    - run: npm --version
    - run: npm install
    - run: npm run build