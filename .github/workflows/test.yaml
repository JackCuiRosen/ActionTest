name: 打包Vue
on: push

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
    - name: 读取仓库内容
      uses: actions/checkout@v4
    - name: 显示路径
      run: pwd
    - name: 显示版本
      run: npm --version
    - name:安装依赖
      run: npm install
    - name: 打包项目
      run: npm run build